package router

const (
	// healthCheckEndpoint holds the URI path to basic health check endpoint
	healthCheckEndpoint = "/v0/health/check"

	// AuthVerifyEndpoint holds the URI path to basic auth check endpoint
	AuthVerifyEndpoint = "/v0/auth/verify"
)
